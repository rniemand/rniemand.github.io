<ol class="list-group blog-list mb-2">
	{%- for post in postslist | reverse %}
	<li class="list-group-item justify-content-between align-items-start" onclick="javascript:location.href='{{ post.url }}'">
		<div class="ms-2">
			<div class="fw-bold d-flex">
				<div>
					<a href="{{ post.url }}" class="postlist-link">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a>
					<div>
						<i class="bi bi-clock"></i>
						<time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate("LLLL yyyy") }}</time>
					</div>
				</div>
				
				<div class="p-2 flex-grow-1">&nbsp;</div>
				{%- if post.data.logo -%}
					<img src="{{post | postLogo(page)}}" alt="logo" width="50" height="100%" />
				{%- endif -%}
			</div>
			
			{%- if post.data.banner -%}
				<img class="mt-1" src="{{post | postBanner(page)}}" alt="banner" width="100%" />
			{%- endif -%}
		</div>
	</li>
	{%- endfor %}
</ol>
